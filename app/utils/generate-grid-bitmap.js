
let CANVAS;
let ctx;

export function init() {
  CANVAS = document.createElement('canvas');
  ctx = CANVAS.getContext('2d');
}

export function(sx, sy, sWidth, sHeight, image) {
  ctx.drawImage()
}

